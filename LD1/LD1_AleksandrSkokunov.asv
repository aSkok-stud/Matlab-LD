% Aleksandr Skokunov
% EKf-25
% Data: 2026-09-14
clear all
clc

x = 1:32;
y = x.^2;

plot(x, y, 'o-r', x, y/3, 'xb')
title('Dvi funkcijos')
xlabel('X-ai')
ylabel('F_1 [-o-]   |   F_2 [-x-]')

%% Dokumentacijos paieška

help sin
doc plot
docsearch title

%% Funkcijų aiškinimas

% linspace - sukuria vektorių nurodytame intervale.
% y = linspace(x1, x2, n)

% size - grąžina masivo dydį
% sz = size(A)
% szdim = size(A,dim)
% szdim = size(A,dim1,dim2,...,dimN)
% [sz1,...,szN] = size(___)

% max - atrenka maksimalų elementą iš masyvo.
% M = max(A)
% M = max(A,[],"all")
% M = max(A,[],dim)
% M = max(A,[],vecdim)
% M = max(A,[],___,missingflag)
% [M,I] = max(___)
% [M,I] = max(A,[],___,"linear")
% C = max(A,B)
% C = max(A,B,missingflag)




